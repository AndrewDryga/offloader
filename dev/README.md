# Dev Tooling

This directory will hold local smoke, benchmark, e2e, and deploy-check scripts.

Initial scripts to add through the task queue:

- `scripts/e2e.sh`
- `scripts/deploy-check.sh`
- `scripts/benchmark.sh`
- `scripts/support-bundle-fixture.sh`
