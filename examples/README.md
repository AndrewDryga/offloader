# Examples

Examples should let a buyer complete the first-hour golden path locally.

Add through the task queue:

- a small non-game Parquet dataset
- a manifest fixture
- endpoint config fixtures
- tenant/key fixtures
- failure lab fixtures for bad manifest, stale dataset, bad param, and forbidden
  tenant
