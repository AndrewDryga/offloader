# Offloader — public marketing + docs. Everything is crawlable.
User-agent: *
Allow: /

# Add once a production domain is set (also add <link rel="canonical"> + og:url in the pages):
# Sitemap: https://<your-domain>/sitemap.xml
